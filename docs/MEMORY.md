# RER Project Memory

This document records durable decisions and context. It is not a changelog or phase tracker. Current phase status lives in `docs/PHASES.md`.

Do not edit this file unless a memory update was explicitly approved.

---

## Business

Rajasthan Estate Realtors (RER) is a family-run real estate consultancy in Mumbai.

Operating since 1988.

Founder / Proprietor:

Mr. Hanif Zamindar

RER has a strong historical connection with Jogeshwari and Mumbai's Western Suburbs.

---

## Brand Direction

Desired positioning:

- trustworthy
- experienced
- local
- premium
- professional
- relationship-driven
- approachable

The website should not feel like a generic real-estate portal.

---

## Design Direction

Approved general direction:

- minimalist
- premium
- readable
- restrained
- modern
- mobile-first

Avoid:

- unnecessary sections
- excessive animations
- clutter
- overly complicated cards
- excessive decorative UI

Homepage section count and hero treatment are not locked. A later redesign may change them after research.

---

## Homepage (current state)

The **Next.js** homepage (`app/(website)/page.jsx`) is the real, live homepage. It is no longer a placeholder and the Vite version in `src/` is retained only as a frozen migration reference — it is not rendered anywhere and nothing in `app/`, `components/`, or `lib/` imports from `src/`.

Current composition:

- Navbar (in layout)
- HeroSlider
- Floating WhatsApp
- PropertySearch
- FeaturedProperties — now data-driven, reading live properties from Payload via `getFeaturedProperties()` (filtered to public statuses + the `featured` tag), not the old hardcoded `src/data/properties.js`
- AboutTeaser
- Footer (in layout)

The homepage will eventually be simplified and improved. A dedicated New Developments section is planned but not yet built — there is no project/development data model yet for it to draw on. Do not rebuild the homepage from scratch during technical migration.

---

## Hero

The hero will eventually be reconsidered.

The preferred **future** direction is professionally generated AI imagery rather than relying on complex HTML-created banner compositions. That is not the current implementation.

Hero imagery must work on mobile as well as desktop.

---

## Properties

Properties should eventually be managed through data rather than hardcoded UI.

The future system should support controlled tags. Example labels (taxonomy not approved):

- New
- Featured
- For Sale
- For Rent
- New Development
- Under Construction
- Ready to Move
- Premium
- Price Reduced

---

## New Developments

A separate concise homepage section is desired for new projects/developments in or near Jogeshwari.

This should not make the homepage unnecessarily long.

---

## Property Pages

Target:

/properties/[slug]

Each property must display the correct property based on the slug.

The current React implementation has a known problem where PropertyDetails uses a fixed property rather than reading the slug.

This must be corrected during migration.

---

## Admin Panel

The admin panel is planned for a later phase.

Do not build it around the temporary static data model.

First finalize:

1. property model
2. tags
3. statuses
4. project/development model
5. database
6. authentication

---

## SEO

SEO is a major reason for moving from the React/Vite SPA to Next.js.

Strategic geographic focus:

- Jogeshwari
- Mumbai Western Suburbs
- relevant genuinely served locations

Do not keyword stuff. Specific keyword maps and location-page architecture are not approved yet.

---

## Competitor Benchmark

Gupta & Sen is a strategic benchmark.

Important benchmark areas:

- location-page architecture
- SEO strategy
- seller lead generation
- trust signals
- market intelligence/report concept
- authority positioning

Do not copy its visual design.

---

## Technology Migration

Durable decision: migrate React/Vite → Next.js App Router.

Working branch: `nextjs-migration`.

`main` remains the stable React/Vite version until migration is verified.

Keep migration and redesign separate. First migrate technically, then redesign strategically.

---

## Git Strategy

Use controlled checkpoints.

Do not make major migration changes without a Git checkpoint.

Never force push.

Do not delete the main/stable implementation until migration has been verified.

---

## Important Existing Decisions

Footer:
Approved and considered satisfactory.

Navbar:
Approved as a starting point.

Migration and redesign:
Keep separate.

---

## Open Decisions

These still require future review:

- final homepage structure
- final hero design
- exact property taxonomy
- exact tag taxonomy
- database choice
- admin technology
- authentication
- lead management
- analytics
- final SEO keyword architecture
- final location-page architecture
- final social media URLs
