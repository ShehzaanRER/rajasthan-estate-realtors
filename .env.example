# Server environment

# PostgreSQL connection used by Payload
DATABASE_URL=

# Payload secret
PAYLOAD_SECRET=

# Google Places API (New) — server-side only. Used for admin Nearby Search.
# Enable Places API (New) on the Google Cloud project. Do not prefix with NEXT_PUBLIC_.
GOOGLE_MAPS_API_KEY=
